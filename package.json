{
  "name": "@danielkalen/notices-engine",
  "version": "2.3.2",
  "description": "A simple notice and subnotice display system",
  "main": "dist/js/notices.js",
  "browser": {
    "./subnotices": "./src/subnotices.coffee",
    "./notices": "./src/notices.coffee",
    "jquery": "./src/_parts/jquery.coffee"
  },
  "author": "danielkalen",
  "license": "MIT",
  "scripts": {
    "postversion": "npm run build && git add . && git commit -m '[Build]'",
    "postpublish": "git push",
    "build": "npm run compile && npm run minify",
    "compile": "npm run compile:notices && npm run compile:subnotices",
    "compile:notices": "simplyimport bundle --umd notify src/notices.coffee > dist/js/notices.js",
    "compile:subnotices": "simplyimport bundle --umd subnotify src/subnotices.coffee > dist/js/subnotices.js",
    "minify": "closure-service dist/js/notices.js > dist/js/notices.min.js && closure-service dist/js/subnotices.js > dist/js/subnotices.min.js",
    "watch": "simplywatch -g 'src/*' -x 'npm run compile -s'"
  },
  "devDependencies": {
    "closure-compiler-service": "^0.5.0",
    "coffee-script": "^1.10.0",
    "simplyimport": "^4.0.0-s17",
    "simplywatch": "^3.0.0-l2",
    "uglify": "*"
  },
  "dependencies": {
    "jquery": "^3.2.1",
    "jquery.transit": "^0.9.12"
  }
}
